<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <context:property-placeholder location="classpath:synchronize-database.properties"/>

    <context:spring-configured />

    <!--  pick up rabbit broker configuration -->
    <bean class="net.peacesoft.CustomDataSource"
          destroy-method="close" id="localConnection">
        <property name="driverClassName" value="${db.driverClassName}" />
        <property name="url" value="${local.url}" />
        <property name="username" value="${local.username}" />
        <property name="password" value="${local.password}" />
    </bean>
    
    <!--  pick up rabbit broker configuration -->
    <bean class="net.peacesoft.CustomDataSource"
          destroy-method="close" id="remoteConnection">
        <property name="driverClassName" value="${db.driverClassName}" />
        <property name="url" value="${remote.url}" />
        <property name="username" value="${remote.username}" />
        <property name="password" value="${remote.password}" />
    </bean>
    
<!--    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean" lazy-init="false">
        <property name="targetObject" ref="remoteConnection" />
        <property name="targetMethod" value="addConnectionProperty" />
        <property name="arguments">
            <list>
                <value>characterEncoding</value>
                <value>UTF-8</value>
            </list>
            <list>
                <value>useUnicode</value>
                <value>true</value>
            </list>
        </property>
    </bean>-->
    
    <bean class="net.peacesoft.repositories.MetaRepository" id="metaRemoteConnection">
        <property name="dataSource" ref="remoteConnection" />
    </bean>
    
    <bean class="net.peacesoft.repositories.MetaRepository" id="metaLocalConnection">
        <property name="dataSource" ref="localConnection" />
    </bean>
    
    <bean class="net.peacesoft.repositories.ContentRepository" id="contentRemoteConnection">
        <property name="dataSource" ref="remoteConnection" />
    </bean>
    
    <bean class="net.peacesoft.repositories.ContentRepository" id="contentLocalConnection">
        <property name="dataSource" ref="localConnection" />
    </bean>
    
    <bean class="net.peacesoft.repositories.DomainRepository" id="domainRemoteConnection">
        <property name="dataSource" ref="remoteConnection" />
    </bean>
    
    <bean class="net.peacesoft.repositories.DomainRepository" id="domainLocalConnection">
        <property name="dataSource" ref="localConnection" />
    </bean>
</beans>